import React from "react";
import Base from "../Base/Base";

const WelcomePage = () => {
  return (
    <Base
      title="Welcome to my app"
      description="We have all Students details"
    >
    </Base>
  );
};

export default WelcomePage;
